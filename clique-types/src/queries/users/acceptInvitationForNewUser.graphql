mutation acceptInvitationForNewUser(
  $invitationId: ID!
  $password: String!
  $code: String!
) {
  acceptInvitationForNewUser(
    invitationId: $invitationId
    password: $password
    code: $code
  ) {
    token
    eventId
  }
}
